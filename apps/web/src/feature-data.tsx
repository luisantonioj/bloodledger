import{useCallback,useEffect,useState}from"react";import{pollingDelay}from"./polling";
type Aggregate={institutionId:string;institutionDisplayName:string;bloodType:string;component:string;inventoryStatus:string;confirmedCount:number;lastProjectedAt:string};type Unit={unitId:string;bloodType:string;component:string;expiresAt:string;inventoryStatus:string;projectedAt:string};type Dashboard={composition:string;scope:string;inventory:Aggregate[];pendingScans:{status:string;count:number}[];lastSuccessfulProjectionAt:string|null;classification:"SIMULATION_ONLY"};type Inventory={scope:string;aggregates:Aggregate[];units:Unit[];classification:"SIMULATION_ONLY"};
async function load<T>(path:string,signal:AbortSignal):Promise<T>{const response=await fetch(path,{credentials:"same-origin",headers:{Accept:"application/json"},signal});const body=await response.json().catch(()=>null)as any;if(!response.ok)throw new Error(body?.error?.message??"The data service is unavailable.");return body as T}
function useLiveData<T>(endpoint:string|null){const[data,setData]=useState<T>(),[error,setError]=useState(""),[busy,setBusy]=useState(false),[retry,setRetry]=useState(0);const manual=useCallback(()=>setRetry(value=>value+1),[]);useEffect(()=>{if(!endpoint)return;let timer:ReturnType<typeof globalThis.setTimeout>|undefined,failures=0,closed=false,attempt=0,controller:AbortController|undefined;const run=async()=>{if(closed||document.hidden)return;const current=++attempt;controller?.abort();controller=new AbortController();setBusy(true);try{setData(await load<T>(endpoint,controller.signal));setError("");failures=0}catch(reason){if(!controller.signal.aborted){failures++;setError(reason instanceof Error?reason.message:"The data service is unavailable.")}}finally{if(!closed&&current===attempt){setBusy(false);timer=globalThis.setTimeout(()=>void run(),pollingDelay(failures))}}};const visible=()=>{globalThis.clearTimeout(timer);if(!document.hidden)void run()};document.addEventListener("visibilitychange",visible);void run();return()=>{closed=true;controller?.abort();globalThis.clearTimeout(timer);document.removeEventListener("visibilitychange",visible)}},[endpoint,retry]);return{data,error,busy,manual}}
const format=(value:string|null)=>value?new Intl.DateTimeFormat("en-PH",{timeZone:"Asia/Manila",dateStyle:"medium",timeStyle:"short"}).format(new Date(value)):"Not yet projected";
function AggregateTable({items}:{items:Aggregate[]}){return items.length===0?<div className="empty"><strong>No committed inventory</strong>No ledger-confirmed projection is available for this scope.</div>:<div className="table-wrap"><table><thead><tr><th>Institution</th><th>Blood type</th><th>Component</th><th>Status</th><th>Confirmed</th><th>Projected</th></tr></thead><tbody>{items.map((item,index)=><tr key={`${item.institutionId}-${item.bloodType}-${item.component}-${item.inventoryStatus}-${index}`}><td>{item.institutionDisplayName}</td><td>{item.bloodType.replaceAll("_"," ")}</td><td>{item.component.replaceAll("_"," ")}</td><td><span className="status">{item.inventoryStatus}</span></td><td>{item.confirmedCount}</td><td>{format(item.lastProjectedAt)}</td></tr>)}</tbody></table></div>}
export function FeatureData({path}:{path:string}){const endpoint=path==="/"?"/api/v1/dashboard":path==="/inventory"?"/api/v1/inventory":null;const state=useLiveData<Dashboard|Inventory>(endpoint);if(!endpoint)return <div className="empty"><strong>Data unavailable</strong>The official feature API is not implemented yet. Runtime mock fallback is disabled.</div>;if(!state.data&&state.busy)return <div className="empty" aria-live="polite"><strong>Loading authorized data</strong>Waiting for the official API.</div>;if(!state.data)return <div className="empty" role="alert"><strong>Unable to load data</strong>{state.error}<br/><button className="button" onClick={state.manual}>Retry</button></div>;if(path==="/"){const data=state.data as Dashboard,total=data.inventory.reduce((sum,item)=>sum+item.confirmedCount,0),pending=data.pendingScans.reduce((sum,item)=>sum+item.count,0);return <><div className="stats"><article><span>Ledger-confirmed</span><strong>{total}</strong></article><article><span>Uncommitted scan states</span><strong>{pending}</strong></article><article><span>Last projection</span><strong className="time">{format(data.lastSuccessfulProjectionAt)}</strong></article></div>{state.error&&<p className="notice" role="status">Showing the last confirmed view. Refresh failed: {state.error} <button className="button" onClick={state.manual}>Retry</button></p>}<AggregateTable items={data.inventory}/></>}const data=state.data as Inventory;if(data.scope==="CITY_AGGREGATE")return <AggregateTable items={data.aggregates}/>;return data.units.length===0?<div className="empty"><strong>No committed units</strong>No ledger-confirmed units are available for this institution.</div>:<div className="table-wrap"><table><thead><tr><th>Unit</th><th>Blood type</th><th>Component</th><th>Status</th><th>Expires</th><th>Projected</th></tr></thead><tbody>{data.units.map(item=><tr key={item.unitId}><td className="mono">{item.unitId}</td><td>{item.bloodType.replaceAll("_"," ")}</td><td>{item.component.replaceAll("_"," ")}</td><td><span className="status">{item.inventoryStatus}</span></td><td>{format(item.expiresAt)}</td><td>{format(item.projectedAt)}</td></tr>)}</tbody></table></div>}
