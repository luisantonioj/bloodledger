export type Aggregate = { institutionId:string; institutionDisplayName:string; bloodType:string; component:string; inventoryStatus:string; confirmedCount:number; lastProjectedAt:string };
export type Unit = { unitId:string; bloodType:string; component:string; expiresAt:string; inventoryStatus:string; projectedAt:string };
export type Dashboard = { composition:string; scope:string; inventory:Aggregate[]; pendingScans:{status:string;count:number}[]; lastSuccessfulProjectionAt:string|null; classification:"SIMULATION_ONLY" };
export type Inventory = { scope:string; aggregates:Aggregate[]; units:Unit[]; classification:"SIMULATION_ONLY" };
export type Alert = { alertId:string; alertType:string; severity:string; unitId:string|null; bloodType:string|null; component:string|null; expiresAt:string|null; evaluatedAt:string; status:string; acknowledged:boolean };
export type AlertAggregate = { institutionDisplayName:string; alertType:string; severity:string; status:string; count:number; lastEvaluatedAt:string };
export type Alerts = { scope:string; alerts:Alert[]; aggregates:AlertAggregate[]; classification:"SIMULATION_ONLY" };
export type Transfer = { transferId:string; sourceInstitutionId:string; destinationInstitutionId:string; bloodType:string; component:string; quantity:number; urgency:string; requestTime:string; status:string; reasonCode:string|null; recommendationDigest:string|null; ledgerVersion:number; projectedAt:string; dispatchEvidenceRecorded:boolean; receiptEvidenceRecorded:boolean };
export type Transfers = { scope:string; transfers:Transfer[]; classification:"SIMULATION_ONLY" };
export type TransferEvent = { eventId:string; fromStatus:string|null; toStatus:string; eventTime:string; reasonCode:string|null; ledgerTransactionId:string; ledgerVersion:number; correlationId:string };
export type AlgorithmExplanation = { runId:string; algorithm:"RPS"|"BROA"; algorithmVersion:string; recommendationEligibility:"DISABLED_UNAPPROVED_POLICY"; evaluationTime:string; configSha256:string; recommendationDigest:string|null; score:number; factors:{name:string;normalized:number;contribution:number}[]; trigger:string|null; selectedUnitId:string|null; automaticApproval:false };
export type TransferDetail = { transfer:Transfer; selectedUnitIds:string[]; timeline:TransferEvent[]; explanations:AlgorithmExplanation[]; selectionPolicy:"FEFO"; recommendationEligibility:"DISABLED_UNAPPROVED_POLICY"; automaticApproval:false; classification:"SIMULATION_ONLY" };
export type TransferSummary = { status:string; transferCount:number; unitCount:number };
export type Consortium = { scope:"CITY_AGGREGATE"; inventory:Aggregate[]; alerts:AlertAggregate[]; transferSummary:TransferSummary[]; lastSuccessfulProjectionAt:string|null; classification:"SIMULATION_ONLY" };
export type AuditEvent = { auditEventId:string; institutionDisplayName:string; actionCode:string; targetType:string; outcome:string; safeErrorCode:string|null; correlationId:string; ledgerTransactionId:string|null; eventTime:string };
export type Audit = { scope:"INSTITUTION"|"CITY_AGGREGATE"; events:AuditEvent[]; classification:"SIMULATION_ONLY" };
export type Report = { reportType:"CITY_INVENTORY_SUMMARY"; scope:"CITY_AGGREGATE"; generatedAt:string; inventory:Aggregate[]; alerts:AlertAggregate[]; transferSummary:TransferSummary[]; disclaimer:string; classification:"SIMULATION_ONLY" };
export type FeatureResponse = Dashboard | Inventory | Alerts | Transfers | Consortium | Audit | Report;
