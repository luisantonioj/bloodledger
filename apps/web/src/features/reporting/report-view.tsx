import { AggregateTable, AlertAggregateTable, TransferSummaryTable } from "../../components/ui/aggregate-tables";
import { formatManilaDateTime } from "../../components/ui/display";
import type { Report } from "../../services/api/types";

export function ReportView({data}:{data:Report}) {
  const units=data.inventory.reduce((sum,item)=>sum+item.confirmedCount,0),alerts=data.alerts.reduce((sum,item)=>sum+item.count,0),transfers=data.transferSummary.reduce((sum,item)=>sum+item.transferCount,0);
  return <><div className="report-hero"><div><span>Prototype evidence package</span><h3>City inventory summary</h3><p>{data.disclaimer}</p><small>Generated {formatManilaDateTime(data.generatedAt)} · Asia/Manila display</small></div><a className="button primary" href="/api/v1/reports/inventory.csv" download>Download simulation CSV</a></div><div className="stats report-summary"><article><span>Confirmed units</span><strong>{units}</strong></article><article><span>Aggregate alerts</span><strong>{alerts}</strong></article><article><span>Transfers</span><strong>{transfers}</strong></article><article><span>Classification</span><strong className="scope">Simulation only</strong></article></div><section className="evidence-section"><header><div><h3>Inventory aggregate</h3><p>Approved committed inventory summary included in the export.</p></div><span>JSON + CSV</span></header><AggregateTable items={data.inventory}/></section><section className="evidence-section"><header><div><h3>Alert aggregate</h3><p>Approved alert summary included as prototype evidence.</p></div><span>Read only</span></header><AlertAggregateTable items={data.alerts}/></section><section className="evidence-section"><header><div><h3>Transfer aggregate</h3><p>Permitted transfer lifecycle counts without operational mutation controls.</p></div><span>Read only</span></header><TransferSummaryTable items={data.transferSummary}/></section></>;
}
