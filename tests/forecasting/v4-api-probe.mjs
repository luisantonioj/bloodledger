// Compatibility entrypoint: the independent verification now uses authenticated
// HTTP and a persisted snapshot/evidence chain in one end-to-end probe.
await import("./v4-independent-verification.mjs");
