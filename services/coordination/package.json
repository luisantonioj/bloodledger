{
  "name": "@bloodledger/coordination",
  "version": "0.1.0",
  "private": true,
  "description": "Simulation-only Sprint 3 location, RPS, and BROA worker",
  "type": "module",
  "main": "build/src/cli.js",
  "bin": {
    "bloodledger-coordination": "build/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test --test-isolation=none build/test/*.test.js"
  },
  "dependencies": {
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  }
}
